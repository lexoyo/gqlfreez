# No `first` argument: gqlfreez walks every page and merges the results.
# This API returns everything in one go, but the query would work the same
# against a paginated one.
{
  countries {
    code
    name
    emoji
    continent {
      name
    }
  }
}
